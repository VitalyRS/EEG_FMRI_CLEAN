# ⚠️ Критерии качества очистки EEG-fMRI и типичные ловушки

## 🎯 Целевые метрики качества (по стадиям)

### Bergen (Gradient Artifact Removal)
✅ **Хорошо:**
- Подавление градиентных гармоник (20, 30, 40, 50, 60 Hz): **≥ 99.5%**
- Сохранение альфа-ритма (8–13 Hz): **≥ 85%**
- Альфа-пик остаётся видимым на затылочных каналах (O1, Oz, O2)

❌ **Плохо:**
- Подавление < 99% → остались градиентные остатки
- Альфа < 80% → Bergen съел реальный сигнал
- Альфа-пик сдвинулся > 1 Hz → неправильный shift/win_k

### BCG (Ballistocardiogram Removal)
✅ **Хорошо:**
- Подавление кардио-полосы (0.7–4 Hz): **≥ 30%**
- Сохранение альфы: **≥ 60%** (допустимо, остаток добьёт ICA)
- Корреляция с ЭКГ упала: **≥ 15%**

⚠️ **Важно:** BCG на данных после Bergen часто **слабо выражен** (Bergen уже частично его подавил). Если BCG-подавление < 30%, это нормально — не пытайся выжать больше агрессивными параметрами, это съест реальный сигнал.

❌ **Плохо:**
- Альфа < 50% → BCG слишком агрессивный
- Подавление BCG 0% при явном пульсовом артефакте на ЭКГ-привязанном усреднении

### ICA (Independent Component Analysis)
✅ **Хорошо:**
- Отклонено компонент: **20–40%** от общего числа
- Сохранение альфы: **≥ 75%**
- Variance drop (удалённая дисперсия): **≤ 30%**
- Удалено каналов (clean_rawdata): **≤ 10%** (≤ 9 из 95)

❌ **Плохо (переочистка):**
- Отклонено > 60% компонент → ICLabel порог слишком низкий
- Variance drop > 50% → удалили реальный сигнал
- Альфа < 60% → ICA съела физиологический ритм
- Удалено > 15% каналов → clean_rawdata слишком агрессивна

❌ **Плохо (недоочистка):**
- Отклонено < 10% компонент → ICLabel порог слишком высокий
- На спектрах остались явные пики мышечной активности (20–40 Hz)
- Остался глазной артефакт (1–3 Hz) на лобных каналах

---

## 🚨 Типичные ловушки

### 1. **Метрика alpha_retention обманывает**
**Проблема:** BCG-артефакт имеет широкополосную мощность, включая 8–13 Hz. Когда OBS/ICA корректно удаляют BCG, часть удалённой мощности в альфа-полосе — это **артефакт, а не реальная альфа**. Метрика засчитывает это как «съели альфу» и ложно штрафует.

**Как проверить правильно:**
- Смотри **форму альфа-пика** на PSD: если пик остался на 9–11 Hz с чёткой prominence — альфа сохранена
- Сравнивай с **референсной записью вне сканера** (EEG21), если она есть
- Если alpha_retention = 65%, но пик виден и острый — это норма, не переочищай дальше

### 2. **ICA отклоняет 70–80% компонент (переочистка)**
**Причина:** ICLabel порог слишком низкий (0.50–0.65). При таком пороге удаляются компоненты с P(Brain) = 0.40–0.55, которые **вероятно мозг**, а не артефакт.

**Решение:**
- Подними `iclabel_thresh` до **0.75–0.85** (консервативный режим)
- При 0.80: удаляются только компоненты, где P(Artifact) ≥ 0.80 — явные артефакты
- Лучше оставить несколько сомнительных компонент, чем выкинуть реальный сигнал

### 3. **clean_rawdata удаляет центральные каналы (Fz, Cz, CPz)**
**Причина:** Параметр `ChannelCriterion` слишком строгий (> 0.85). Центральные каналы имеют меньшую корреляцию с соседями из-за разных источников сигнала, но они **не плохие**.

**Решение:**
- Используй `ChannelCriterion = 0.70–0.80` (не выше!)
- Или отключи его совсем (`'off'`) и удаляй только flatline-каналы
- Если удалено > 10% каналов — параметры слишком агрессивны

### 4. **Рост мощности после 50 Hz на "чистых" данных**
**Причины:**
1. **Остаточный артефакт 91 Hz** (крио-насос МРТ) — ICLabel не поймал его как Line Noise
2. **Алиасинг от реконструкции** — после удаления 70% ICA-базиса остаток плохо представляет исходный сигнал
3. **Интерполяция удалённых каналов** размазала их шум обратно на соседей

**Решение:**
- Снизь агрессивность ICA (порог 0.80, меньше удалённых компонент)
- Для 91 Hz: добавь узкий notch-фильтр 88–94 Hz **до** ICA (если артефакт стабильный)
- Проверь, не удалено ли слишком много каналов (clean_rawdata)

### 5. **Variance drop 70% — это катастрофа, а не очистка**
**Что это значит:** Удалено 70% всей дисперсии сигнала. Остались только высокочастотный шум + пара brain-компонент.

**Целевое значение:** variance_drop ≤ 30%
- 10–20% — лёгкая очистка (удалены только явные артефакты)
- 20–30% — умеренная очистка (норма для EEG-fMRI)
- 30–50% — агрессивная (риск потери реального сигнала)
- > 50% — переочистка (данные испорчены)

---

## 📋 Чек-лист правильной очистки

### После Bergen (step 05-07)
- [ ] Градиентные гармоники подавлены ≥ 99.5%
- [ ] Альфа-пик виден на O1/Oz/O2 в диапазоне 8–13 Hz
- [ ] Alpha retention ≥ 85% (или пик сохранён визуально)
- [ ] Нет артефактных остатков на 20/30/40/50/60 Hz

### После BCG (step 08-09)
- [ ] Кардио-полоса (0.7–4 Hz) подавлена ≥ 20%
- [ ] Альфа retention ≥ 60% (или пик всё ещё виден)
- [ ] Корреляция с ЭКГ упала ≥ 10%
- [ ] Если BCG-подавление < 20%, это допустимо (Bergen уже частично подавил)

### После ICA (step 10-11)
- [ ] Отклонено 20–40% компонент (не больше!)
- [ ] Variance drop ≤ 30%
- [ ] Alpha retention ≥ 70%
- [ ] Удалено ≤ 10% каналов (clean_rawdata)
- [ ] Альфа-пик всё ещё виден на затылочных каналах
- [ ] Нет роста мощности > 50 Hz (кроме известных артефактов типа 91 Hz)

### Красные флаги (STOP и пересмотри параметры)
- 🚩 Отклонено > 60% ICA-компонент
- 🚩 Variance drop > 50%
- 🚩 Alpha retention < 60%
- 🚩 Альфа-пик исчез или сдвинулся > 1.5 Hz
- 🚩 Удалено > 15% каналов
- 🚩 Центральные каналы (Fz, Cz, CPz) удалены как "bad"

---

## 🛠️ Рекомендованные параметры

### Bergen (step 04-05)
```json
{
  "shift": 10–30,
  "win_k": 15–30,
  "motion_thresh": 0.5–1.0
}
```

### BCG (step 08-09)
**Не используется в текущей версии** — step08 использует MATLAB fMRIB OBS, а не Optuna.

### ICA (step 10-11)
```json
{
  "flatline_crit": 5.0,
  "channel_crit": 0.75,       // не выше 0.80!
  "line_crit": 4.0,
  "iclabel_thresh": 0.80      // консервативный порог
}
```

### Если данные сильно зашумлены (много движений):
```json
{
  "channel_crit": 0.70,       // чуть мягче
  "iclabel_thresh": 0.75      // чуть агрессивнее
}
```

### Если данные чистые (мало движений):
```json
{
  "channel_crit": 0.80,       // можно строже
  "iclabel_thresh": 0.85      // очень консервативно
}
```

---

## 🔍 Как диагностировать проблему

### Симптом: Альфа-пик исчез
**Возможные причины:**
1. Bergen слишком агрессивный (shift слишком мал, win_k слишком велик)
2. BCG порог слишком низкий
3. ICA удалила brain-компоненты (порог < 0.70)

**Диагностика:**
- Посмотри PSD **до и после каждой стадии**
- Если альфа пропала после Bergen → перезапусти Optuna Bergen
- Если альфа пропала после ICA → подними iclabel_thresh до 0.85

### Симптом: Отклонено 70% ICA-компонент
**Причина:** ICLabel порог слишком низкий (0.50–0.65)

**Решение:**
```bash
rm data/1916/segments/segment04/ica_optuna_best.json
# Отредактируй step11_ica_final.py → дефолт iclabel_thresh = 0.80
python step11_ica_final.py --segment-dir data/1916/segments/segment04
```

### Симптом: Удалено > 10 каналов (clean_rawdata)
**Причина:** ChannelCriterion слишком строгий (> 0.85)

**Решение:**
- Отредактируй `ica_optuna_best.json`: `"channel_crit": 0.75`
- Или отключи детекцию каналов совсем и оставь только ICA

### Симптом: Рост мощности после 50 Hz
**Причина:** Переочистка (удалено слишком много компонент) + алиасинг

**Решение:**
1. Подними iclabel_thresh → меньше удалённых компонент
2. Проверь, не удалено ли > 10% каналов
3. Если есть стабильный пик 91 Hz — добавь notch-фильтр до ICA

---

## 📊 Пример хороших метрик (segment04)

### После Bergen:
```
G_suppression:     99.98%
Alpha retention:   85%
Alpha peak:        9.8 Hz (O1, Oz, O2)
Status:            PASS
```

### После BCG:
```
Cardiac suppression:  29%
Alpha retention:      61%
Corr drop:            16%
Status:               REVIEW (норма для слабого BCG)
```

### После ICA:
```
IC rejected:        25 / 87  (29%)
Variance drop:      22%
Alpha retention:    78%
Channels removed:   5 / 95   (5%)
Status:             GOOD
```

---

## 🎓 Главный принцип

> **Лучше оставить немного артефакта, чем удалить реальный сигнал.**

EEG-fMRI очистка — это баланс, а не гонка за 100% подавлением артефактов. Цель — получить данные, пригодные для анализа, а не идеально чистые, но пустые.

Если сомневаешься — будь консервативнее. Недоочистку можно исправить на следующей стадии, переочистку — нельзя.
